import { FilterPipeCarColorPipe } from './filter-pipe-car-color.pipe';

describe('FilterPipeCarColorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeCarColorPipe();
    expect(pipe).toBeTruthy();
  });
});
